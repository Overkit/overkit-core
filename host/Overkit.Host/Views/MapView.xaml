<UserControl
    x:Class="Overkit.Host.Views.MapView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Overkit.Host.Views">

    <Grid ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="320" />
        </Grid.ColumnDefinitions>

        <Border Background="#14141E" CornerRadius="8" Padding="4">
            <Viewbox Stretch="Uniform">
                <Canvas x:Name="MapCanvas" Width="800" Height="800" />
            </Viewbox>
        </Border>

        <Grid Grid.Column="1" RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <AutoSuggestBox x:Name="SpeciesBox" PlaceholderText="Espèce à farmer (ex. Cotileau…)" />
            <ToggleSwitch x:Name="TimeGate" Grid.Row="1" OffContent="Ignorer jour/nuit"
                          OnContent="Respecter jour/nuit" IsOn="True" />
            <StackPanel Grid.Row="2" Spacing="4">
                <TextBlock x:Name="StatusText" Opacity="0.7" TextWrapping="Wrap" />
                <HyperlinkButton x:Name="ClearTargetButton" Content="Retirer la cible du HUD"
                                 Visibility="Collapsed" Padding="0" FontSize="12"
                                 Click="ClearTarget_Click" />
            </StackPanel>

            <ListView x:Name="SpotList" Grid.Row="3" SelectionMode="None" IsItemClickEnabled="False">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:SpotRow">
                        <Grid Padding="0,6" ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" FontSize="13" />
                                <TextBlock Text="{x:Bind Detail}" Opacity="0.6" FontSize="11" />
                            </StackPanel>
                            <Button Grid.Column="1" Content="🎯" FontSize="13" Padding="8,4"
                                    Click="Target_Click" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
