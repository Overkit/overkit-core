<Window
    x:Class="Overkit.Host.PanelWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:Overkit.Host.Views"
    Title="Overkit">

    <Grid Padding="24,12,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Overkit" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.5" Margin="0,0,0,8" />

        <NavigationView
            x:Name="Nav"
            Grid.Row="1"
            PaneDisplayMode="Top"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            SelectionChanged="Nav_SelectionChanged"
            Margin="0,0,0,10">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Palbox" Tag="palbox" IsSelected="True" />
                <NavigationViewItem Content="Checklist de craft" Tag="craft" />
                <NavigationViewItem Content="Accouplement" Tag="breeding" />
                <NavigationViewItem Content="Carte" Tag="map" />
                <NavigationViewItem Content="＋ Créer une card" Tag="editor" />
            </NavigationView.MenuItems>
        </NavigationView>

        <Grid x:Name="Pages" Grid.Row="2">
            <views:PalboxView x:Name="Palbox" />
            <views:CraftView x:Name="Craft" Visibility="Collapsed" />
            <views:BreedingView x:Name="Breeding" Visibility="Collapsed" />
            <views:MapView x:Name="Map" Visibility="Collapsed" />
            <views:CardEditorView x:Name="Editor" Visibility="Collapsed" />
        </Grid>
    </Grid>
</Window>
